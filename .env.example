# =============================================================================
# Environment Variables Template
# =============================================================================
#
# IMPORTANT: 
# 1. Copy this file to '.env' to use it locally: cp .env.example .env
# 2. Never commit the .env file to version control (it's in .gitignore)
# 3. This file contains demo credentials for the Shopify Mock Shop, allowing
#    the project to work immediately after cloning without connecting to a real store.
#
# QUICK START OPTIONS:
# ====================
# Option A - Demo Mode (No Shopify account needed):
#   1. cp .env.example .env
#   2. npm run dev
#
# Option B - Connect to Your Store (Recommended):
#   1. cp .env.example .env
#   2. npx shopify hydrogen link
#   3. This will automatically configure all required environment variables
#
# Option C - Manual Configuration:
#   See GETTING-STARTED.md for detailed instructions
#
# =============================================================================

# =============================================================================
# REQUIRED: Session Secret
# =============================================================================
# Required for the app to work. Used to sign session cookies.
# 
# For development: "foobar" works fine
# For production: Generate a secure random string:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
SESSION_SECRET="foobar"

# =============================================================================
# Shopify Store Configuration
# =============================================================================
# Demo credentials for Mock Shop - works immediately after cloning
# Replace these with your actual store credentials when connecting to a real store

PUBLIC_STORE_DOMAIN="mock.shop"
PUBLIC_STOREFRONT_API_TOKEN="3b580e70970c4528da70c98e097c2fa0"
PUBLIC_STOREFRONT_API_VERSION="2024-01"

# =============================================================================
# Additional Variables (Auto-configured by Shopify CLI)
# =============================================================================
# When you run 'npx shopify hydrogen link' or 'npx shopify hydrogen env pull',
# the following variables will be automatically added by the Shopify CLI:
#
# PRIVATE_STOREFRONT_API_TOKEN="..."
# PUBLIC_STOREFRONT_ID="..."
# PUBLIC_CUSTOMER_ACCOUNT_API_CLIENT_ID="..."
# PUBLIC_CUSTOMER_ACCOUNT_API_URL="..."
# PUBLIC_CHECKOUT_DOMAIN="..."
#
# If configuring manually, you can add these variables here.
# See GETTING-STARTED.md for instructions on how to obtain these values.
# =============================================================================

# =============================================================================
# Optional Configuration
# =============================================================================

# Custom menu handles (defaults to 'main-menu' and 'footer' if not set)
# Create these menus in Shopify Admin → Online Store → Navigation
# PUBLIC_HEADER_MENU_HANDLE=main-menu
# PUBLIC_FOOTER_MENU_HANDLE=footer

# =============================================================================
# Shopify Admin API Credentials (Optional - for MCP/Admin Operations)
# =============================================================================
# These are only needed if you're using Shopify Admin API features or MCP.
# You can get these from Shopify Admin → Settings → Apps and sales channels → Develop apps
#
# SHOPIFY_API_KEY="your-api-key"
# SHOPIFY_API_SECRET="your-api-secret"
# SHOPIFY_ADMIN_API_TOKEN="your-admin-api-token"
# =============================================================================

# =============================================================================
# Security Notes
# =============================================================================
# - Never commit this file or the .env file to version control
# - Never share tokens in messages, emails, or public repositories
# - If a token is compromised, revoke it immediately in Shopify Admin
# - Use secure SESSION_SECRET in production
# =============================================================================
